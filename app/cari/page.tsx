import type { Metadata } from 'next';
import Link from 'next/link';
import { Search } from 'lucide-react';
import { PageHero } from '@/components/PageHero';
import { publicSearchIndex } from '@/lib/content';
export const metadata: Metadata = { title: 'Pencarian', description: 'Cari program dan informasi Yayasan Ruang Sejahtera.' };
export default async function SearchPage({ searchParams }: { searchParams: Promise<{ q?: string }> }) { const { q = '' } = await searchParams; const query = q.trim().toLowerCase(); const results = query ? publicSearchIndex.filter((item) => `${item.title} ${item.description}`.toLowerCase().includes(query)) : []; return <><PageHero eyebrow="Pencarian" title="Temukan informasi dengan cepat." description="Pencarian V2 menggunakan indeks konten publik dan dapat dikembangkan ke berita, kegiatan, galeri, laporan, dan dokumen ketika sumber datanya tersedia." /><section className="py-18 md:py-24"><div className="shell"><form action="/cari" method="get" className="flex max-w-3xl gap-2"><label htmlFor="search" className="sr-only">Kata kunci</label><div className="relative flex-1"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-neutral-400" size={19} /><input id="search" name="q" defaultValue={q} placeholder="Cari program atau halaman..." className="min-h-12 w-full rounded-xl border border-neutral-300 bg-white pl-12 pr-4 text-sm" /></div><button className="button-primary" type="submit">Cari</button></form>{query ? <div className="mt-10"><p className="mb-5 text-sm text-neutral-500">{results.length} hasil untuk “{q}”</p><div className="grid gap-3">{results.map((item) => <Link key={item.href} href={item.href} className="border border-neutral-200 p-5 hover:border-red-200"><h2 className="font-bold text-brand-ink">{item.title}</h2><p className="mt-2 text-sm leading-6 text-neutral-600">{item.description}</p></Link>)}</div></div> : <p className="mt-8 text-sm text-neutral-500">Masukkan kata kunci untuk memulai pencarian.</p>}</div></section></>; }
